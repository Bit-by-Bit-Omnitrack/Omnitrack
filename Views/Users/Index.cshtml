<<<<<<< Updated upstream
﻿@model IEnumerable<UserRoles.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-block btn-primary"> <i class="fa fa-plus"></i> Create New</a>
</p>
<table class="table table-bordered table-striped">

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfBirth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th>Edit</th>
            <th>Details</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfBirth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a> 
                </td>
                <td>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning">Details</a>
                </td>
                <td>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                <td>
            </td>
        </tr>
}
    </tbody>
</table>
=======
﻿@model IEnumerable<UserRoles.Models.User> // This view expects a collection (IEnumerable) of User objects from the UserRoles.Models namespace

@{
    ViewData["Title"] = "Index"; // Setting the title of the page to 'Index'
}

<!-- Button to navigate to the 'Create' action for adding a new employee -->
<p>
    <a asp-action="Create" class="btn btn-block btn-primary">
        <i class="fa fa-plus"></i> Add new Employee
    </a>
</p>

<!-- Card layout for styling the user list -->
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Omnitrack Users List</h3> <!-- Header for the card -->
    </div>

    <div class="card-body">
        <!-- User list table -->
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <!-- Display column headers dynamically from the model's properties -->
                    <th>@Html.DisplayNameFor(model => model.FirstName)</th>
                    <th>@Html.DisplayNameFor(model => model.LastName)</th>
                    <th>@Html.DisplayNameFor(model => model.PhoneNumber)</th>
                    <th>@Html.DisplayNameFor(model => model.EmailAddress)</th>
                    <th>@Html.DisplayNameFor(model => model.Role)</th>
                    <th>Edit</th>
                    <th>View</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                <!-- Loop through each user in the model collection and display their data -->
                @foreach (var item in Model)
                {
                    <tr>
                        <!-- Display user properties -->
                        <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                        <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                        <td>@Html.DisplayFor(modelItem => item.PhoneNumber)</td>
                        <td>@Html.DisplayFor(modelItem => item.EmailAddress)</td>
                        <td>@Html.DisplayFor(modelItem => item.Role)</td>

                        <!-- Edit button linking to the 'Edit' action with the user's ID -->
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                        </td>

                        <!-- View button linking to the 'Details' action -->
                        <td>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning">View</a>
                        </td>

                        <!-- Delete button linking to the 'Delete' action -->
                        <td>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
>>>>>>> Stashed changes
